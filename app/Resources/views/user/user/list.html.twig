<div class="row">
    <div class="col-xs-12">
        {% if is_granted('USER_USER_CREATE') %}
            <a href="{{ path('user_edit') }}" class="btn bg-green waves-effect">{{ 'user.user.add.title'|trans }}</a>
        {% endif %}
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
        <div class="body table-responsive">
            <form action="#">
                <table class="table table-striped dataTable">
                    <thead>
                    <tr>
                        <th>#</th>
                        <th data-paginator-sortname="username" data-paginator-sort="{{ order.username }}" class="sorting{% if order.username != '' %}_{{ order.username }}{% endif %}">{{ 'user.user.list.table.userName'|trans }}</th>
                        <th data-paginator-sortname="email" data-paginator-sort="{{ order.email }}" class="sorting{% if order.email != '' %}_{{ order.email }}{% endif %}">{{ 'user.user.list.table.email'|trans }}</th>
                        <th data-paginator-sortname="group" data-paginator-sort="{{ order.group }}" class="sorting{% if order.group != '' %}_{{ order.group }}{% endif %}">{{ 'user.user.list.table.group'|trans }}</th>
                        <th>{{ 'app.common.table.action'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for user in results %}
						<tr>
                            <td scope="row">{{ user.id }}</td>
                            <td>{{ user.userName }}</td>
                            <td>{{ user.email }}</td>
                            <td>{{ user.group.label }}</td>
                            <td>
                                <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-warning material-icons">edit</a>
                                <a href="#" data-id="{{ user.id }}" class="btn btn-danger material-icons delUser">delete</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </form>
            <div class="pull-right">
                {% include 'main/pagination.html.twig' %}
            </div>
        </div>
    </div>
</div>
